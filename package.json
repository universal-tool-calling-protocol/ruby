{
  "name": "ruby-utcp-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node ./serve.mjs",
    "start": "node ./serve.mjs",
    "check": "node --check app.js && node --check data.js && node --check build.mjs && node --check serve.mjs",
    "build": "node ./build.mjs",
    "preview": "node ./serve.mjs --dir dist"
  }
}
